[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn api.app:app --host 0.0.0.0 --port 8000 --workers 1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/data"
