# Contributing

1. Keep changes **constraints-first**.
2. Do not weaken RED constraints.
3. Add tests or validator checks when adding new required fields.
4. Update `CHANGELOG.md`.
5. Regenerate `packet_manifest.yaml` (SHA-256) after modifications.
