# REFINEMENTS -- 7-aspect multi-agent refining pass (Workflow w8cxhpu0k, 8 agents, 2026-06-13)

Each finding verified against the REAL artifacts (code, corpus, live site). Work the prioritized list top-down. map-never-launder.

## EXECUTION LOG
- **#1 DONE (HIGH -- moat crack) 2026-06-13.** Fixed the removable-singularity false-positive in src/concordance_engine/verifiers/mathematics.py verify_equality (_pole_bases + _domain_mismatch: re-parse raw strings evaluate=False so x/x is not auto-reduced; flag when the two sides' pole-sets DIFFER -> MISMATCH, not a clean HOLDS). Validated standalone, then against the REAL verifier (6 singularity cases -> MISMATCH; 14 legit identities incl. symmetric rationals -> still CONFIRMED). Deployed (server-side syntax-check + restart + backup mathematics.py.bak). Verified LIVE: x/x==1 / (x^2-1)/(x-1)==x+1 / sin/sin==1 now BROKEN; sin^2+cos^2 + symmetric rationals still HOLDS; benchmark intact. Added 5 singularity + 1 symmetric-rational regression cases to tools/benchmark_public_verify.py (now 58/58, 0 false-pos). NOTE: the bogus seal 73b78c81 (x/x==1) already minted is permanent; new ones are now prevented. Honest scope: covers the denominator/pole class; deeper function-domain (sqrt/log branch) cases + applying the same guard to verify_derivative/integral are future refinements.
- **#4 + #5 DONE (HIGH -- security) 2026-06-13.** #4 compute-DoS: added _ast_compute_guard in mathematics.py _parse (AST pre-check before sympify: rejects literal exponents > 10000, power TOWERS like 9**9**9 = 9**(9**9), and oversized/too-deep expressions; thresholds well above any legit expr). Validated standalone + against the real verifier (9**9**9 / 2**99999999 / towers rejected instantly; 58 benchmark exprs + golden-ratio/Euler still verify). #5 rate-limit: wired _rate_check(request,'derivation') into derivation_verify + _rate_check(request,'derivation_solve') into derivation_solve (added request:Request params) + added _RATE_LIMITS entries derivation (120/min, generous since compute is now guarded) + derivation_solve (12/min, paid oracle). Deployed BOTH files in ONE restart (server-side syntax-check both + .bak2 backups). VERIFIED LIVE: benchmark 58/58 0-false; normal verify HOLDS; 9**9**9 -> INCOMPLETE in <1s (was a multi-second 369M-digit bignum hang); 200 CONCURRENT calls -> 123x200 + 77x429 (limit fires under flood, legit sequential use unaffected).
- **#2 + #3 DONE (HIGH/MEDIUM -- verdict laundering) 2026-06-13.** #2: re-verdicted 30 cards (the 25 bridge_* + scholar_lemaitre + scholar_mendel + connection_dna_is_the_language_of_life + connection_maxwell_rule + connection_the_narrow_way) from HOLDS -> STRUCTURAL, adding proof_verdict:HOLDS so the math kernel stays machine-verified but the bare label no longer reads as 'theology proven by math'. Discriminator (clean + principled): HOLDS + theology_doctrine/scripture domain + NO cite_url (no stored seal) -> change; HOLDS + a real cite_url seal (give-to-live, vN-ordinals, phyllotaxis, play-captures-force, rotary-motor -- science-first, earned) -> LEFT. Deployed entries.jsonl (LF-normalized; 1622 rows) + verified the sample bridge is now STRUCTURAL live. #3: added HOLDS ('math kernel machine-verified -- the form, not the meaning') + STRUCTURAL ('a structural/canonical parallel, not a proof') to site/almanac.html verdict legend (const meaning + const order); deployed + verified live. The second HIGH finding (the metadata seam) is closed.

## EXECUTIVE SUMMARY
The system is genuinely strong where it matters most and honest about most of its edges ? but two HIGH issues touch the moat itself and must lead the queue. (1) THE MOAT HAS ONE REAL CRACK: the live /derivation/verify seals removable-singularity falsehoods as HOLDS ? confirmed live, a permanent seal certifying x/x==1 as HOLDS with integrity_verified=true now exists (seal 73b78c81...). This is the core promise ("the engine never seals a falsehood") broken in production via SymPy's simplify silently cancelling poles. No corpus card exploits it, but the door is open. (2) THE STRUCTURED VERDICT FIELD LAUNDERS THEOLOGY: ~28-30 bridge/scholar/connection cards carry verdict:HOLDS while their titles assert a theological identity ("'I am the way' IS the triangle inequality"); the seals and card bodies correctly fence HOLDS to the arithmetic, but the bare metadata label + an almanac legend that has no HOLDS/STRUCTURAL entry means a reader scanning verdicts sees "theology proven by math" ? the exact misread the entire discipline exists to prevent. Both are protectable in days. Beyond those, the deepest pattern is DRIFT: hand-maintained counts (README "seven domains", agents.html "298 almanac entries" vs live 1622, llms.txt giving three different tool counts) contradict the live /capabilities everywhere, and the headline vine-validity 1.000 is propped to ~6x by the very kin/truss braces the metric was built to forbid (true concord-bond reach is 0.170). The honesty discipline in the engine and the wisdom prose is exemplary; the failures are at the seams ? the verdict label, the public counts, and one parser path ? not the heart.

## CROSS-CUTTING THEMES
- STALE HAND-MAINTAINED COUNTS (appeared in 4 aspects: honesty, surfaces, mission, corpus). README 'seven domains/eleven tools', agents.html '298 almanac entries' (live 1622), and llms.txt's three different tool counts all contradict the live /capabilities. The structural cure is the same everywhere: make /capabilities the single source of truth and have every other surface fetch or build-inject from it rather than restating numbers ? so the 'computed live, conduit not source' credibility can never decay.
- THE BARE VERDICT LABEL OUTRUNS THE EVIDENCE THE PROSE FENCES (theology, honesty, corpus all flagged it). The seals and the wisdom/body text are exemplary map-never-launder; the failure is consistently at the structured metadata layer ? verdict:HOLDS on theological-identity cards with no legend entry to scope it. The fix is to make the boundary travel WITH the label (legend entries, a proof_verdict field, scoped 'HOLDS ? math figure only', tooltipped BOUNDARY lines), not just live in the body a reader may never open.
- SELF-FLATTERING METRICS ? the rigor standard applied inward. Two headline numbers are propped above what the honest measure supports: vine-validity 1.000 (really 0.170 on concord bonds, inflated ~6x by the kin/truss braces the metric forbids) and the benchmark's '0 false-positives' (real, but its FALSE set has no domain-exclusion decoys, so it missed the x/x=1 crack). 'Rigor is concordance' must be turned on the project's own scoreboards: report the honest primary number, keep the flattering one as clearly-secondary, and add the adversarial cases that would have caught the gap.
- THE MOAT IS STRUCTURALLY HIDDEN / MIS-FRAMED (verification, surfaces, mission). The un-copyable asset ? deterministic verification + the permanent re-checkable seal + the proof-bridges wall ? is simultaneously the moat, the .org notary product, and the tell-a-friend artifact, yet it has a live correctness crack (#1), is buried behind one homepage link, blocked from search by robots.txt, and not led-with in the README. Investing in the seal as a first-class, correct, human-legible, citable object compounds fidelity, adoption, and the registry thesis at once.
- COMPUTE & WRITE-PATH HARDENING LAGS THE READ-PATH DISCIPLINE (security, verification). Parsing safety, secrets hygiene, and rate limiting on ~30 endpoints are genuinely strong, but the two derivation endpoints ? the most compute-heavy and the ones that mint permanent seals ? uniquely lack rate limiting, timeouts, AST guards, and input-list bounds. The same SymPy surface is both the DoS vector and the false-seal vector; hardening it (#1, #4, #5) protects correctness and availability together.

## PER-ASPECT HEALTH
- **Theological & doctrinal soundness ? scripture/theology cards** -- Strong. Across ~300 theology/scripture cards the honesty discipline genuinely holds: the apex/Person/Col 1:17 join is reserved everywhere I sampled, original-language Greek/Hebrew is accurate (not strained), gematria is handled as math-vs-exegesis rather than doctrine, resonance is attributed, and the engine's own theology verifier never emits a math HOLDS. The one real systemic seam is presentational: 30 theology-tagged cards carry a card-level verdict label of HOLDS whose meaning is not in the public verdict legend ? the card bodies scope it correctly to the arithmetic, but the bare label is the weak point.
- **Verification integrity ? the moat (does the engine ever seal** -- Solid-with-one-real-crack. The moat is genuinely rigorous ? 52/52 benchmark with 0 false seals, all 377 cited seals resolve with integrity_verified=true, math kernels are real and the theology is honestly kept in attributed prose (HOLDS attaches to the math, not the scripture) ? but adversarial probing found a reproducible false-positive: the live endpoint seals removable-singularity identities (x/x=1) as HOLDS.
- **Honesty & rigor ? the clean mirror ? across the corpus AND t** -- Solid-with-gaps. The hard honesty machinery is genuinely strong ? the seal layer verifies math-only and never launders theology (confirmed live), the benchmark docs flag their own limits, and the wisdom text reserves the apex faithfully. The gaps are at the seams between layers: the card-level `verdict: HOLDS` field carries a math verdict onto theological identity-claim titles, and the public copy has a few unearned absolutes plus inconsistent self-counts across surfaces.
- **Corpus quality & coherence ? data/almanac/entries.jsonl (162** -- Solid-with-gaps: the substrate is clean (0 malformed lines, 0 duplicate ids, HOLDS discipline genuinely intact) but the CONNECTIVE tissue is weak and the headline metric is misleading ? concord-bond vine-validity is 0.170, not the reported 1.000, with 116 broken bonds the map silently hides.
- **Live surfaces & accessibility (human + agent)** -- Solid-with-gaps ? the live engine and its core flows genuinely work end-to-end and the honesty discipline holds under test (false claims return BROKEN, seals are integrity-verified), but agent-facing documentation surfaces carry stale and mutually contradictory counts, and extensionless clean URLs 404 site-wide.
- **Security & robustness of the exposed verification engine (/d** -- Solid-with-gaps: injection surface and secrets hygiene are genuinely well-handled, but compute-DoS is a real unmitigated hole and the derivation endpoints uniquely lack the rate-limiting that every other public write path already has.
- **Mission & strategic coherence ? does the whole hang together** -- Solid-with-gaps. The mission, the architecture (two trees / the Gate / the wedge / the reserved apex), and all three live faces are genuinely coherent and mutually reinforcing ? but the FRONT-DOOR framing is split three ways (README, .com hero, wedge doc each describe a different product), and the un-copyable moat is structurally hidden from the humans who would adopt it.

## PRIORITIZED REFINEMENTS (the queue)

### #1  [HIGH / medium]  (Verification integrity (the moat))
**Refinement:** Fix the removable-singularity false-positive in src/concordance_engine/verifiers/mathematics.py verify_equality: after simplify(ea-eb)==0, inspect the un-cancelled difference (sympy.denom(together(ea-eb)) / continuous_domain) and, if simplify discarded poles or removable singularities, return a domain-qualified verdict ('holds except at {excluded points}') instead of an unconditional HOLDS ? and never mint an unqualified seal for it. Add x/x=1, (x^2-1)/(x-1)=x+1, tan=sin/cos as ground-truth FALSE/qualified rows in tools/benchmark_public_verify.py.
**Why:** This is the one true crack in the moat and the core promise broken in production ? verified live: a permanent seal (73b78c81...) certifies x/x==1 as HOLDS with integrity_verified=true. The whole enterprise rests on 'the engine never seals a falsehood'; until this lands, that claim is false on the live endpoint.

### #2  [HIGH / medium]  (Honesty & rigor)
**Refinement:** Fix the ~28-30 theology cards (bridge_*, scholar_lemaitre/mendel, connection_* with theology_doctrine) that carry verdict:HOLDS on 'X IS Y' identity titles: downgrade the card verdict to STRUCTURAL/CONCORDANT and move the math proof to a separate field (proof_verdict:HOLDS / seal_url), OR keep HOLDS but scope it explicitly ('HOLDS ? math figure only') and soften 'X IS Y' titles to 'X is figured by / resonates with Y'. The seal already verifies math-only; make the card metadata match the seal's honesty.
**Why:** This is the corpus's own stated red line: the structured verdict field is the one place that launders theology, stamping a machine-proof label on a theological assertion. An agent or reader scanning verdicts gets exactly the 'theology proven by math' misread the discipline exists to prevent. Doctrine-protecting and high-leverage.

### #3  [MEDIUM / small]  (Theology & doctrine)
**Refinement:** Add HOLDS and STRUCTURAL to the public verdict legend in site/almanac.html (const meaning) with fencing copy ? HOLDS: 'math kernel machine-verified ? the form, not the meaning'; STRUCTURAL: 'a structural/canonical parallel, not a proof'. Additionally surface each theology_doctrine HOLDS card's own 'BOUNDARY (honest)' sentence in the verdict chip/tooltip so the fence travels with the label wherever cards are listed or filtered.
**Why:** Highest leverage-per-effort on the doctrine seam: it closes the read-path side of issue #2 directly (a reader filtering by verdict currently sees a theological title stamped HOLDS with no legend entry telling them HOLDS means math-only). Small CSS/JS + copy change that makes the boundary the card bodies already state travel with the bare label everywhere.

### #4  [HIGH / medium]  (Security & robustness)
**Refinement:** Close the 9**9**9 / deep-expression compute-DoS class with an AST pre-check in mathematics.py:_parse (parse with ast, reject ast.Pow whose literal exponent exceeds ~1000 and reject node-count over a cap) BEFORE sympify ? cheap, no infra change, blocks the unbounded-bignum class immediately. Follow with the durable fix: run each step's verify in a ProcessPoolExecutor with future.result(timeout~2s) so hostile compute can actually be killed (win32 + threadpool means SIGALRM won't work and threads can't be cancelled).
**Why:** A single 8-char request can stall the entire single-worker production server (/health, /seal, the whole site) with a ~369M-digit bignum and no time/memory bound ? flagged open in PRE_TRAFFIC_SECURITY.md item 3. The AST reject is small and immediate; the ProcessPool is the structural fix.

### #5  [HIGH / small]  (Security & robustness)
**Refinement:** Wire the two derivation endpoints into the existing rate limiter: add _rate_check(request,'derivation') to derivation_verify (api/app.py:5643) and derivation_solve (5683) with a registry entry like 'derivation': (12, 12.0/60), and gate seal:true through the stricter existing 'seal' bucket as well. Also bound the attacker-controlled variables list (<=26, validate names ^[A-Za-z][A-Za-z0-9_]{0,15}$) and claimed_solutions (<=64) in the app-layer guard at api/app.py:5659.
**Why:** These are the only public write endpoints with NO rate limit at all (every other path calls _rate_check), and they are the most compute-expensive ? directly amplifying the DoS in #4 and the seal-spam in #1. The infrastructure already exists; this is wiring it in. Small effort, closes a clear hole.

### #6  [HIGH / medium]  (Corpus quality & coherence)
**Refinement:** Make coordinate_map.py report concord-bond vine-validity (0.170, bonds only) as the PRIMARY number and the kin-assisted reach (1.000) as secondary ('concord vine-validity 0.170 | structural reach 1.000 with kin braces'). Add tools/check_bonds.py (load all ids, scan every bonds[] entry, emit card_id -> missing_target) and wire it into the seal/write pipeline so a bond to a non-existent id is rejected at write time.
**Why:** The headline 1.000 overstates corpus coherence ~6x and is achieved by the exact kin/truss/legalism pattern the VINE measure was created to replace (per feedback_vine_not_truss memory) ? the metric is currently flattering itself. The bond checker would have caught all 116 broken refs the map silently hides; both restore the honesty the measure is supposed to embody.

### #7  [HIGH / medium]  (Live surfaces & accessibility)
**Refinement:** Make documentation counts live or build-time-injected from /capabilities rather than hand-typed, and correct the current stale values now: agents.html (298 -> 1622 almanac entries, plus tool/verifier/domain counts), llms.txt (remove the contradictory '48 total' and '63 verifiers' lines; cite /capabilities), and README (seven domains / eleven tools -> live numbers). Make GET /capabilities the single source of truth every other surface references.
**Why:** Stale counts appeared in FOUR aspects (honesty, surfaces, mission, corpus). agents.html under-reports the almanac ~5.4x and llms.txt gives three different tool counts on the single most important agent-discovery document ? directly suppressing the adoption the project names as its binding constraint, and undercutting the 'rigor is concordance / counts computed live' credibility on the exact pages meant to win trust.

### #8  [MEDIUM / small]  (Mission & strategic coherence)
**Refinement:** Re-lead README.md with the wedge as sentence one ('machine-checks a derivation across ~66 domains and mints a permanent, independently-checkable seal ? the verification floor an AI agent reaches for; it also runs Scripture-grounded decision packets through four gates'), demoting the decision-packet architecture to a clearly-labelled second section. One product, two faces ? say so explicitly.
**Why:** The single front-door document describes a different (2024-era decision-packet) product than the live derivation+seal moat, so a developer landing on GitHub may never reach the real adoption lever. A reorder + one framing sentence, no new functionality. Pairs naturally with the README count fix in #7.

### #9  [MEDIUM / small]  (Mission & strategic coherence)
**Refinement:** Add one family-framed human-facing link/tile from the .com homepage (site/index.html) to proof-bridges.html ? e.g. 'A wall of wonders: words of Christ tied to proven form, with a receipt you can open' ? keeping the technical /mcp link separate. And resolve robots.txt Disallow:/seal deliberately: allow the curated ~16 proof-bridge + benchmark exemplar seals (or inline the seal content with JSON-LD on the crawlable proof-bridges.html) so the re-checkable proofs are findable by search.
**Why:** The proof-bridges wall is the project's own-identified deepest, most un-copyable, most shareable asset, yet it is hidden from the family audience most likely to forward it and blocked from the search discovery path BOTTLENECKS.md names as the binding constraint. One link + one robots decision converts existing traffic into reach for the moat.

### #10  [MEDIUM / small]  (Live surfaces & accessibility)
**Refinement:** Add host-level clean-URL routing so /<page> serves or 301-redirects to /<page>.html site-wide (Cloudflare Pages '/* -> /:splat.html' style), and ship a friendly custom 404 linking back to /welcome.html. Regenerate sitemap.xml from the real page set (add registry.html, giving.html) with <lastmod> dates to honor the llms.txt 'recrawl daily' guidance.
**Why:** Extensionless URLs 404 site-wide (/proof, /verify-seal, /agents all dead) ? a near-universal habit for both humans trimming '.html' and agents guessing URLs hits a dead end with no fallback. Config-level fix, broad reach.

### #11  [MEDIUM / small]  (Honesty & rigor)
**Refinement:** Tighten README line 204's unearned absolute: state the claim the math supports ? 'Checking a claim against a fixed external authority is O(1) and does not grow with participant count, unlike all-pairs consensus polling which is O(n^2)' ? and drop 'more efficient than any consensus-based approach at any scale' (it ignores gossip/leader-based designs). Also replace the dated '52/52 ... zero false seals' embedded in openapi.json's permanent info.description with a pointer to /capabilities or BENCHMARK_PUBLIC_VERIFY.md so the spec can't silently age.
**Why:** The 'rigor is concordance' standard applies to the project's claims about itself; an unearned absolute and a dated number baked into a permanent spec are the two clearest places the public copy outruns the argument actually made. Small copy edits, protects the honesty brand.

### #12  [LOW / small]  (Theology & doctrine)
**Refinement:** Rescope verify_salvation_by_grace in src/concordance_engine/verifiers/theology_doctrine.py (~lines 190-205) so it reads as a plain-text consistency check on Eph 2:8-9 ('not of works') with a one-line note that the faith-works relationship (James 2) is outside this text-check ? and/or emit a STRUCTURAL/scoped verdict rather than a bare CONFIRMED/MISMATCH. Separately, review the thinnest-kernel HOLDS bridges (bridge_salt_is_halted_decay, whose only sealed fact is e^0=1) and either downgrade to CONCORDANT/STRUCTURAL or tighten the body to name exactly what is sealed.
**Why:** The file's own header promises to pick no side on denominational distinctives, yet this check edges toward adjudicating the Protestant vs Catholic/Orthodox faith-and-works dispute. And bridge_salt_is_halted_decay is the case where the label/kernel asymmetry is largest. Both are small honesty-tightening edges, not violations.

### #13  [MEDIUM / medium]  (Corpus quality & coherence)
**Refinement:** Using the new check_bonds.py: apply a one-time id-drift remap for the ~54 fixable broken bonds (som_01 -> teaching_som_*, beatitude_N -> beatitude_N_<suffix>, teaching_talents -> teaching_parable_talents, teaching_treasure_and_pearl -> teaching_parable_treasure_and_pearl); move the ~6 free-text/prose entries (hyphenated phrases, 'A<->B (note)') out of bonds[] into the connections field; and disambiguate the 106 missing-verdict cards by adding an explicit 'ROOT'/'RESERVED' sentinel to intentionally-unverdicted teaching cards and backfilling verdicts on the 18 saying cards via the existing saying verifier.
**Why:** These are the concrete repairs that follow once the checker (rank 6) exists: they restore the bond graph (116 broken refs, 6 malformed prose entries) and remove the ambiguity between 'intentionally un-verdicted root' and 'verdict missing by oversight'. Mechanical, well-scoped, and they make the corpus self-validating going forward.

### #14  [LOW / small]  (Security & robustness)
**Refinement:** Reconcile CORS with PRE_TRAFFIC_SECURITY.md item 5: either scope allow_origins (api/app.py:173) to the known verify/seal/identity/capabilities surfaces, or ? the honest map-not-launder move ? accept the deliberate wildcard and update the doc to say so (low-risk given allow_credentials=False prevents the wildcard+credentials combo). Document the decision either way so doc and code agree.
**Why:** Genuinely low browser-CSRF risk (no cookies in scope, credentials disabled), but the code is broader than the documented intent ? a doc/code mismatch that the rigor standard says to resolve explicitly rather than leave implicit.

### #15  [LOW / small]  (Live surfaces & accessibility)
**Refinement:** Soften the .com homepage 'nothing tracked' phrase to 'no ads, no accounts, no behavioral tracking' to match privacy.html (which says requests may be logged for security/abuse), add a 'Last updated: <date>' line to privacy.html, and confirm proof.html's live Verify widget actually posts to /derivation/verify and renders the trail ? giving it one pre-baked worked example with a resolvable /seal/<hash> fallback (as ten-second-proof.html already has) so the page proves itself even if the JS errors.
**Why:** Minor copy-vs-policy tension and an unverified central widget on a page whose whole claim is 'machine-checked live on this page'. Low impact but cheap, and the fallback-seal pattern is already proven elsewhere on the site.

## HIGH-SEVERITY ISSUE DETAILS (where + exact fix)

- **[Verification integrity ? ] src/concordance_engine/verifiers/mathematics.py:76-78 (verify_equality) -> live https://narrowhighway.com/derivation/verify**
  - problem: Removable-singularity / symbolic-cancellation FALSE-POSITIVE. verify_equality decides truth via `simplify(ea - eb) == 0`, and SymPy's simplify silently cancels removable singularities and poles. So claims that are FALSE on a measure-zero exclusion set return HOLDS. Confirmed live: x/x==1 (false at x=0), (x**2-1)/(x-1)==x+1 (false at x=1), x*(1/x)==1, (x-1)/(x-1)==1, sin(x)/sin(x)==1, log(x)/log(x)==1, and tan(x)==sin(x)/cos(x) (poles) ALL return HOLDS. Critically, with seal:true the endpoint MINTED A PERMANENT SEAL for x/x==1: https://narrowhighway.com/seal/73b78c819c15f72d142b8487016bffb6328bc0fdcee432b033c2a3f2e8d3e8c3 (verdict HOLDS, integrity_verified would be true). This is the core promise broken ? the engine sealed a falsehood ? even though no existing corpus card exploits it. A motivated agent could cite a permanent 'HOLDS' receipt for a domain-incorrect identity.
  - fix: After `simplify(ea-eb)==0`, add a domain guard before confirming: compute the singularities/denominators of (ea-eb) BEFORE cancellation (e.g. sympy.denom of together(ea-eb), or sympy.calculus.util.continuous_domain over Reals) and, if the naive difference has poles/removable singularities that simplify discarded, return a domain-qualified verdict (e.g. CONFIRMED_ALMOST_EVERYWHERE / 'holds except at {excluded points}') rather than unconditional HOLDS ? never an unqualified seal. Add these exact cases (x/x=1, (x^2-1)/(x-1)=x+1, tan=sin/cos) to benchmark_public_verify.py as ground-truth FALSE/qualified so the regression is caught.

- **[Honesty & rigor ? the cle] data/almanac/entries.jsonl ? 28 cards: 25 bridge_* (e.g. bridge_the_way_is_the_triangle_inequality, bridge_i_am_is_the_identity_element, bridge_my_words_remain_is_the_invariant, bridge_yes_be_yes_is_the_bit) + 2 scholar_* (scholar_lemaitre_..., scholar_mendel_...) + 3 connection_* with theology_doctrine (connection_the_narrow_way_is_the_path_of_least_resistance, connection_maxwell_rule_the_body_fitly_joined_is_the_rigid_frame, connection_dna_is_the_language_of_life)**
  - problem: These cards carry verdict:HOLDS while their domains include theology_doctrine/scripture and their TITLE asserts a theological identity with 'IS' (e.g. "'I am the way' (Jn 14:6) IS the triangle inequality", "'Before Abraham was, I AM' IS the identity element"). The doctrine reserves HOLDS for machine-verified math; theology gets STRUCTURAL/CONCORDANT. The live seal proves only the math half (verified: seal fd209ae verifies sqrt(3^2+4^2)<=3+4 and nothing theological), but the card-metadata verdict field attaches HOLDS to the whole identity claim. An agent or reader scanning verdicts sees a machine-proof stamp on a theological assertion ? the one place the verdict field outruns the evidence the wisdom text is careful to fence.
  - fix: Split the verdict on these cards so the field cannot be read as proving the theology. Either (a) change the card verdict to STRUCTURAL or CONCORDANT and record the math HOLDS in a separate field (e.g. proof_verdict:HOLDS / seal_url), or (b) keep HOLDS but scope it explicitly in the verdict/title (e.g. 'HOLDS (math figure only)') and soften titles from 'X IS Y' to 'X resonates with / is figured by Y'. The seal already does the right thing; make the card metadata match it.

- **[Corpus quality & coherenc] tools/coordinate_map.py lines 145-155 (vine_validity) and stdout 'VINE-VALIDITY 1.000'**
  - problem: The reported VINE-VALIDITY 1.000 is computed over edges+kin, where kin = 1584 auto-generated same-coord.family braces loaded from data/codex/kin_edges.json. On the actual CONCORD BONDS alone, vine-validity is 0.170 (only 276 of 1622 cards reach a source; 1346 cards / 83% have ZERO concord bonds). Matt's own memory (feedback_vine_not_truss) says the kin/same-family braces ARE the truss/self-stress/legalism pattern the VINE measure was created to REPLACE ('never lateral-brace, never fake a graft'). So the 1.000 is achieved by the exact mechanism the metric forbids ? the headline overstates corpus coherence ~6x.
  - fix: Report TWO numbers and make the bond-only one primary: 'concord vine-validity 0.170 (bonds only) | structural reach 1.000 (with kin braces)'. Treat the gap between them as the work queue ? each tick, graft one of the 1346 orphan cards up to source with a REAL concord bond, never by adding kin.

- **[Corpus quality & coherenc] data/almanac/entries.jsonl bonds[] across 74 cards (e.g. assessment_galilei_book_of_nature, uncertainty_entropy_coconfirm); top targets som_01 (30x), som_01_the_beatitudes (14x)**
  - problem: 116 broken bond references (target id does not exist in corpus). ~54 are pure id-DRIFT: bonds were written against an old scheme and the cards were later renamed/split without updating refs ? som_01 -> teaching_som_*, teaching_beatitude_5 -> teaching_beatitude_5_merciful, teaching_treasure_and_pearl -> teaching_parable_treasure_and_pearl, teaching_talents -> teaching_parable_talents. The coordinate_map silently filters these (line 130 'b in pos'), so they never surface as a problem and the broken count is invisible.
  - fix: Add a scriptable bond-integrity checker (tools/check_bonds.py): load all ids, scan every bonds[] entry, emit (card_id -> missing_target). Then apply a one-time remap table for the ~54 drift cases (som_01->teaching_som_*, beatitude_N->beatitude_N_<suffix>, etc.). Wire the checker into CI / the seal pipeline so a bond to a non-existent id is rejected at write time.

- **[Live surfaces & accessibi] https://narrowhighway.com/agents.html vs live /capabilities**
  - problem: The agent on-ramp page carries several stale counts that contradict the live engine. agents.html states '100 MCP tools / 70 deterministic domains / 298 almanac entries / 11,000+ cards' while live /capabilities reports 102 mcp_tools, 71 verifiers, 66 verifier_domains, and 1622 almanac_entries. The almanac number is off by ~5.4x (298 vs 1622). An agent reading agents.html to plan its integration gets materially wrong numbers, which undercuts the 'counts computed live, conduit not source' credibility on the very page meant to win agent trust.
  - fix: Make agents.html render its counts from a live fetch of /capabilities (or inject them at build time from the same source) instead of hard-coding. At minimum, correct the four numbers now: 102 MCP tools, 71 verifiers, 66 domains, 1622 almanac entries.

- **[Live surfaces & accessibi] https://narrowhighway.com/llms.txt (lines ~156, 158, 422, 520)**
  - problem: llms.txt is internally self-contradictory on tool/verifier counts. Line 156 says 'exposes ~80 tools'; line 158 says '69 domain verifiers'; line 422 says 'Tools available via MCP (48 total)'; line 520 says '63 verifiers across 7 axes'. Live /capabilities says 102 MCP tools and 71 verifiers. So the single most important agent-discovery document gives at least three different MCP-tool counts (~80, 48, and implicitly 102) and three verifier counts (69, 63, 71). An agent cannot tell which is authoritative.
  - fix: Pick one authoritative source (/capabilities) and make llms.txt cite it by reference rather than restating raw numbers, or regenerate llms.txt from the engine so all four spots agree. Remove the contradictory '48 total' and '63 verifiers' lines.

- **[Security & robustness of ] api/derivation.py:104 verify_derivation -> verify_step -> sympy; entered via api/app.py:5670 (no timeout anywhere in this path)**
  - problem: Compute-DoS is unmitigated. The char blacklist does NOT include '*', so a short-but-explosive input like {"expr_a":"9**9**9","expr_b":"0"} (8 chars, well under the 2000-char cap) reaches sympify with the default evaluate=True and forces CPython to materialize a ~369-million-digit integer ? CPU- and memory-bound, GIL-holding, with no wall-clock or memory bound. The length cap and step cap do NOT bound this (PRE_TRAFFIC_SECURITY.md item 3 explicitly names 9**9**9 and is still open). The 100-step cap makes it worse: one request can queue 100 such expressions. Production runs `uvicorn api.app:app --workers 1` (confirmed local/go_live.ps1:81, local/fix_task.ps1:29), and the endpoint is a sync `def` so it runs in the threadpool ? but a GIL-holding bignum op stalls ALL threads/endpoints (/health, /seal, the whole site) until it finishes. Same exposure on /derivation/solve and the per-domain verify_* dispatch.
  - fix: Add an AST/size pre-check before sympify in mathematics.py:_parse: parse with ast, walk it, and reject any ** (ast.Pow) whose exponent is a literal > a small bound (e.g. 1000) and reject expression node-count over a cap. Pair it with a hard wall-clock budget per step that can actually kill the work. Because signal.SIGALRM is POSIX-only and the server is win32 + threadpool, the safe fix is to run each step's verify in a ProcessPoolExecutor with future.result(timeout=~2s) (a process can be terminated; a thread cannot) and also set a child memory cap (resource.setrlimit on Linux deploy). Minimum viable mitigation today: the AST power/size reject (cheap, no infra change) ? it closes the 9**9**9 class immediately.

- **[Security & robustness of ] api/app.py:5643 derivation_verify and 5683 derivation_solve (no _rate_check call; no 'derivation' key in _RATE_LIMITS at 13507)**
  - problem: The two most compute-expensive public endpoints are the ONLY public write endpoints with no rate limit at all. Every other write path calls _rate_check (propose/seal/agent/verify/...); derivation calls none, and there is no default-deny ? _rate_check is simply never invoked, so a single IP can issue unlimited sympy-heavy requests. This directly amplifies the compute-DoS above and contradicts PRE_TRAFFIC_SECURITY.md items 3-4.
  - fix: Add `_rate_check(request, "derivation")` to both handlers (add `request: Request` to the signatures) and a registry entry like `"derivation": (12, 12.0/60)` (heavy path, mirror the 'polymathic'/'agent' tier). Add a stricter bucket when seal:true is set (sealing mints a permanent record) ? e.g. gate `seal` through the existing 'seal' bucket in addition.

- **[Mission & strategic coher] README.md lines 1-8 vs docs/THE_WEDGE.md vs live /identity and /derivation/verify**
  - problem: The single front-door document (README) describes a DIFFERENT product than the live moat. README opens: 'Constraint-based decision architecture grounded in Scripture... runs every decision through four checkpoints' and the whole top third is about decision PACKETS, RED/FLOOR/BROTHERS/GOD gates, and governance. But the actual wedge (THE_WEDGE.md), the live endpoint, the ChatGPT Action, the benchmarks, and /identity are about DERIVATION verification + the permanent seal. An agent or developer landing on the GitHub README meets the 2024-era 'decision-packet validator' framing first and may never reach the 'verify a derivation in one call -> permanent receipt' moat that is the real adoption lever. The two framings are not contradictory in fact, but they are not unified ? the README never says 'these are two faces of one engine'.
  - fix: Re-lead the README with the wedge in one sentence ('The Concordance Engine machine-checks a derivation across ~66 domains and mints a permanent, independently-checkable seal ? the verification floor an AI agent reaches for; it also runs Scripture-grounded decision packets through four gates'), so the derivation/seal moat is the FIRST thing a reader meets. Demote the decision-packet architecture to a clearly-labelled second section. One product, two surfaces ? say so explicitly.

- **[Mission & strategic coher] site/index.html (live narrowhighway.com homepage) ? the single 'For developers & AI ->' link**
  - problem: The most-trafficked public surface buries the entire moat behind ONE small bottom-of-page link. The proof-bridges 'wall' ? which THE_WEDGE.md itself calls the deepest, most un-copyable, most shareable artifact and the project's best tell-a-friend hook ? is NOT linked from the family homepage at all. The wedge doc's own thesis is that 'verifiable claims forward themselves' and 'delight + relief is the tell-a-friend artifact'; yet the family audience (the people most likely to actually forward a 'time dilation is a triangle, here's the receipt' surprise to a friend) is given no path to it. This is the family-facing/agent-facing split tipping from a strength (clean audience separation) into a fracture (the shareable moat is hidden from the largest set of potential sharers).
  - fix: Add ONE audience-appropriate human-facing tile/link on the .com homepage to proof-bridges.html, framed for the family reader, not the developer ? e.g. 'A wall of wonders: words of Christ tied to proven form, with a receipt you can open.' Keep the technical /mcp link separate. This costs one link and converts the homepage's traffic into reach for the un-copyable asset without compromising the family tone.