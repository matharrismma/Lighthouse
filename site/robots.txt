# Concordance — narrowhighway.com
# A deterministic verification engine. The site is optimized for AI
# agents — read /llms.txt first, then /manifest for the full tool list.
# The verified record is crawlable: /almanac/book (every tested claim) and
# /seal/{hash} (each proof, server-rendered with ClaimReview JSON-LD).

User-agent: *
Allow: /
Disallow: /_archive/
Disallow: /auth/
Disallow: /journal/
Disallow: /shelf/
Disallow: /confess
Disallow: /capture
Disallow: /cas/
Disallow: /agent/training
Disallow: /swarm/
Disallow: /keeping/
Disallow: /audit-chain/
Disallow: /chain/
Disallow: /peers
Disallow: /queue
Disallow: /reflect
Disallow: /seal/render
Disallow: /seal/polymathic
Disallow: /verify/
Disallow: /visits/

# AI + search crawlers — explicitly welcome, with the SAME guardrails. A named
# user-agent group does NOT inherit the rules above, so the disallows are
# repeated here (otherwise these crawlers could reach /cas/, /journal/, etc.).
# The seal proof pages (/seal/{hash}) are deliberately left crawlable.
User-agent: GPTBot
User-agent: ClaudeBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Google-Extended
User-agent: Googlebot
User-agent: bingbot
User-agent: CCBot
User-agent: cohere-ai
User-agent: FacebookBot
Allow: /
Disallow: /_archive/
Disallow: /auth/
Disallow: /journal/
Disallow: /shelf/
Disallow: /confess
Disallow: /capture
Disallow: /cas/
Disallow: /agent/training
Disallow: /swarm/
Disallow: /keeping/
Disallow: /audit-chain/
Disallow: /chain/
Disallow: /peers
Disallow: /queue
Disallow: /reflect
Disallow: /seal/render
Disallow: /seal/polymathic
Disallow: /verify/
Disallow: /visits/

# Public read-only surfaces an agent should know about
# /llms.txt              — service description (start here)
# /almanac/book          — the whole tested record, one crawlable page
# /seal/{hash}           — each proof, server-rendered (ClaimReview JSON-LD)
# /agents/daily.json     — daily heartbeat: substrate stats + recent decisions + endpoint catalog (start here for cold-crawl)
# /manifest              — OpenAI-compatible tool list
# /docs                  — OpenAPI schema
# /benchmark             — accuracy results
# /almanac               — curated wisdom + engine-discovered patterns
# /atlas                 — verifier × axis grid (the four-gate chain)
# /witness-gate/health   — Deut 19:15 enforcement summary
# /witness-gate/card/{id}— per-card witness chain
# /ledger                — permanent verification record
# /ledger/verify         — chain integrity check
# /identity              — Ed25519 instance public key + manifesto
# /live.html             — 24/7 channel page
# /channels/narrow-highway/mrss.xml      — channel MRSS feed
# /channels/narrow-highway/epg.xml       — XMLTV EPG
# /channels/narrow-highway/roku_feed.json — Roku Direct Publisher feed
# /feed/walks.rss        — Atlas walks as RSS (subscribeable)
# /.well-known/security.txt
# /.well-known/agents.txt

Sitemap: https://narrowhighway.com/sitemap_index.xml
