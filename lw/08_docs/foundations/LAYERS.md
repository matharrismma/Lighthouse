# Layers & Interfaces (Current)

## Layer 0 — WORD (Authority Reference)
- Full Bible text edition (target: WEB for ease; KJV if licensing required)
- RED has priority; LAW is floor; WAY is optimal.

## Layer 1 — KERNEL (Universal Physics)
Immutable nouns: WORD/RED/LAW/WAY; GATE/FLOOR/WITNESS/WAIT; VESSEL/RULE/ACTION/STATE/LEDGER.

## Layer 2 — KEEPER (Lighthouse OS persona)
- Holds Word locally
- Runs Biblical Alignment Protocol (Four Gates)
- Produces: anchors + directives
- Never speaks as God; never makes new doctrine.

Interface: Problem → (anchors, directives, status)

## Layer 3 — STEWARD (Execution plane)
- Applies actions inside floors
- Writes append-only ledger packets
- Enforces physical/logical firewall boundaries where present.

Interface: directives/actions → state transitions + ledger commits

## Layer 4 — VESSEL (Local node)
- Runs domain adapter (econdev, coach, etc.)
- Local constraints + reserve rules + quarantine.

Interface: adapter dynamics + kernel actions → local outcomes

## Layer 5 — LIGHTHOUSE (Hub)
- Keeper + Steward together
- Coordinates across vessels
- Witness + wait gating for contributions and canonical changes.

Interface: SUB/WIT/DEC ledgers + sync (offline-first)

## On-ramps / Off-ramps (Data)
On-ramps: USB-C, microSD, local WiFi (gated), mesh packets, file import.
Off-ramps: export packets (JSONL), microSD, USB, optional sync.

Principle: **check before share**; never bypass gates.
