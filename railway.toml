[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn api.app:app --host 0.0.0.0 --port $PORT --workers 1'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/data"
